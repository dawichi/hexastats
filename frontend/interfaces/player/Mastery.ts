/**
 * ## Mastery information
 * Shows points, name, image and level of a mastery from a champ.
 */
export interface Mastery {
    name: string
    image: string
    level: number
    points: number
}