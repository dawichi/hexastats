import { getStats } from 'utils'

describe('[getStats.ts]: returns a object with the stats calculated', () => {
    test.todo('Should return the acc of the games, double kills and pentas')
    test.todo('Should return all other stats calculated by nº of games with each champ')
})
